<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.application.report.mapper.CommentReportWriteMapper">
    <insert id="insertCommentReport" parameterType="Map">
        INSERT INTO comment_report_history (accountId, commentId)
        VALUES (#{accountId}, #{commentId})
    </insert>

</mapper>